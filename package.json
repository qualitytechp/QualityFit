{
  "name": "qualityfit",
  "version": "1.0.0",
  "private": true,
  "description": "QualityFit landing page with SCSS tokens and scroll animations.",
  "scripts": {
    "build:css": "sass src/scss/main.scss styles.css --no-source-map",
    "watch:css": "sass --watch src/scss/main.scss:styles.css --no-source-map"
  },
  "devDependencies": {
    "sass": "^1.80.0"
  }
}
